{
  "name": "postgres-export",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "convert": "rm -rf tables && mkdir tables && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Giorgio Delgado",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^4.0.0",
    "pg": "^7.4.0"
  }
}
